apiVersion: v1
kind: Service
metadata:
    name: "fskubecluster-3c6c"
    labels:
        app: "fskubecluster-3c6c"
spec:
    type: ClusterIP
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "fskubecluster-3c6c"