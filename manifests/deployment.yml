apiVersion : apps/v1beta1
kind: Deployment
metadata:
  name: "fskubecluster-3c6c"
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app: "fskubecluster-3c6c"
    spec:
      containers:
        - name: "fskubecluster-3c6c"
          image: "fskubecr.azurecr.io/fskubecluster"
          ports:
          - containerPort: 8080